# GoN2N UI Conventions

- App UI text uses the global Flutter theme font family: `Microsoft YaHei UI`.
- New labels, buttons, form text, and status text should inherit the theme font.
- Do not set one-off Chinese fonts unless a component has a specific rendering need.
- Log text may keep a monospace font for readability.
